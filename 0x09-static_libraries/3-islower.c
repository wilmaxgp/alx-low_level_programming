#include "main.h"

/**
 * _strncat - function that program prints the lowercase alphabet.
 * @c: ASCII equivalent integer
 * Description: Program prints the lower case alphabet using
 * _puthchar and a for loop over the ascii table character set.
 * Return: 1 if lower case or 0 if not..
 */
int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
